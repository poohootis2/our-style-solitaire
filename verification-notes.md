# Latest visual verification

- Desktop 1280x720: dynamic underwater landscape background loaded correctly; chapter/stage badge, roster monster, companion, cards, and controls rendered without overlap.
- Portrait 390x844: dynamic portrait background and all cards fit above the bottom controls; monster and companion are visible. The compact portrait HUD is visually dense, so keep the monster info width restrained and avoid expanding the status row when adding future assets.
- Initial portrait capture before Metro refresh was blank; after server restart and full bundle completion the screen rendered correctly.
